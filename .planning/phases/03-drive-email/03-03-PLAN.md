---
phase: 03-drive-email
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .github/workflows/fesr_scraper.yml
autonomous: true
requirements:
  - AUTO-03
  - AUTO-04
  - AUTO-05

must_haves:
  truths:
    - "pip install includes google-auth google-api-python-client"
    - "Drive upload step runs upload_drive.py and is conditioned on GDRIVE_SA_JSON secret"
    - "Email step runs notify_email.py and is conditioned on SMTP_HOST secret"
    - "Phase 3 placeholder comments replaced by active steps"
---

<objective>
Activate the Phase 3 steps in the GitHub Actions workflow.

Purpose: Plans 03-01 and 03-02 created the scripts. This plan wires them into the workflow:
adds Drive/email packages to pip install, replaces the Phase 3 placeholder comments with
real steps conditioned on secrets being set.

Output: .github/workflows/fesr_scraper.yml with active Drive and email steps.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update fesr_scraper.yml — pip install + activate Phase 3 steps</name>
  <files>.github/workflows/fesr_scraper.yml</files>
  <read_first>
    .github/workflows/fesr_scraper.yml — full file to locate pip install line and Phase 3 comment blocks.
  </read_first>
  <action>
    1. In the "Installa dipendenze" step, change:
         run: pip install requests beautifulsoup4
       to:
         run: pip install requests beautifulsoup4 google-auth google-api-python-client

    2. Replace the Phase 3 Drive placeholder comment block with an active step:
         - name: "Carica su Google Drive"
           if: ${{ secrets.GDRIVE_SA_JSON != '' }}
           run: python upload_drive.py
           env:
             GDRIVE_SA_JSON: ${{ secrets.GDRIVE_SA_JSON }}
             GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
             FESR_OUTPUT: data

    3. Replace the Phase 3 email placeholder comment block with an active step:
         - name: "Invia email di riepilogo"
           if: ${{ secrets.SMTP_HOST != '' }}
           run: python notify_email.py
           env:
             SMTP_HOST: ${{ secrets.SMTP_HOST }}
             SMTP_PORT: ${{ secrets.SMTP_PORT }}
             SMTP_USER: ${{ secrets.SMTP_USER }}
             SMTP_PASS: ${{ secrets.SMTP_PASS }}
             EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
             FESR_OUTPUT: data

    Place the two new steps AFTER the "Carica output" artifact step.
    Keep the "Carica output" step unchanged.
  </action>
  <verify>
    YAML syntax: python -c "import yaml; yaml.safe_load(open('.github/workflows/fesr_scraper.yml')); print('YAML valid')"
    grep "google-auth" .github/workflows/fesr_scraper.yml
    grep "upload_drive.py" .github/workflows/fesr_scraper.yml
    grep "notify_email.py" .github/workflows/fesr_scraper.yml
    grep "GDRIVE_SA_JSON" .github/workflows/fesr_scraper.yml
    grep "SMTP_HOST" .github/workflows/fesr_scraper.yml
    No "Phase 3 placeholder" lines remain.
  </verify>
  <done>
    .github/workflows/fesr_scraper.yml has active Drive and email steps, conditioned on secrets.
    pip install includes google-auth packages. Phase 3 placeholder comments removed.
  </done>
</task>

</tasks>

<success_criteria>
- pip install line includes google-auth and google-api-python-client
- Active "Carica su Google Drive" step runs upload_drive.py
- Active "Invia email di riepilogo" step runs notify_email.py
- Both steps conditioned on their respective secrets
- YAML is valid
</success_criteria>

<output>
Create .planning/phases/03-drive-email/03-03-SUMMARY.md when done.
</output>
