---
phase: 01-date-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delibere.py
autonomous: true
requirements:
  - DATE-01
  - DATE-02

must_haves:
  truths:
    - "scraperesults() accepts datada and dataa keyword params, both defaulting to None"
    - "When datada/dataa are None the function behaves identically to the current implementation"
    - "When datada/dataa are provided, the portal payload carries those dates and only matching delibere are returned"
    - "Bad date strings (wrong format) raise ValueError before any HTTP call is made"
  artifacts:
    - path: "delibere.py"
      provides: "Extended scraperesults, buildpayload, buildpageparams, fetchfirstpage, fetchpage, writesummaries"
      contains: "def scraperesults"
  key_links:
    - from: "delibere.py:scraperesults()"
      to: "delibere.py:buildpayload()"
      via: "datada/dataa passed as positional or keyword args"
      pattern: "buildpayload.*datada"
    - from: "delibere.py:scraperesults()"
      to: "delibere.py:buildpageparams()"
      via: "datada/dataa passed as positional or keyword args"
      pattern: "buildpageparams.*datada"
---

<objective>
Expose the portal's existing date-range filter fields through delibere.py's public API.

Purpose: buildpayload() and buildpageparams() already contain dataAdozioneDa/DATAADOZIONEDA keys
but hardcode empty strings. This plan wires those keys to real values by adding datada/dataa
parameters all the way from scraperesults() down to the two fetch helpers, with DD/MM/YYYY
validation and None-means-no-filter semantics.

Output: delibere.py with date-range filtering fully plumbed; writesummaries() also extended
to accept an optional date suffix token so scraper.py can pass it in Phase Plan 02.
</objective>

<execution_context>
@C:\Users\edoba\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edoba\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-date-filtering/01-CONTEXT.md

<interfaces>
<!-- Existing signatures in delibere.py that this plan modifies. -->
<!-- Do NOT re-read the whole file — use these extracts. -->

Current buildpayload (line 50):
  def buildpayload(anno, keyword) -> dict
  Keys already present: "dataAdozioneDa": "", "dataAdozioneA": ""

Current buildpageparams (line 67):
  def buildpageparams(anno, keyword, page) -> dict
  Keys already present: "DATAADOZIONEA": "", "DATAADOZIONEDA": ""

Current fetchfirstpage (line 88):
  def fetchfirstpage(session, anno, keyword, timeout) -> str
  Calls: session.post(BASE_URL, data=buildpayload(anno, keyword), ...)

Current fetchpage (line 94):
  def fetchpage(session, anno, keyword, page, timeout) -> str
  Calls: fetchfirstpage(...) or session.get(BASE_URL, params=buildpageparams(...), ...)

Current scraperesults (line 258):
  def scraperesults(anno, keyword, maxpages=None, delay=0.4, includelinks=False,
                    download=False, maxpdf=None, timeout=30, outputfolder="data") -> list[dict]
  Calls: fetchpage(session, anno, keyword, page, timeout)

Current writesummaries (line 343):
  def writesummaries(records, anno, keyword, folder) -> None
  Computes suffix = f"{anno}_{keyword.lower()}".replace(" ", "_")
  Writes four CSV files using that suffix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add datada/dataa to buildpayload, buildpageparams, fetchfirstpage, fetchpage</name>
  <files>delibere.py</files>
  <read_first>
    delibere.py lines 50-99 — current signatures and call sites for the four functions being modified.
  </read_first>
  <behavior>
    - buildpayload(anno, keyword, datada=None, dataa=None): sets "dataAdozioneDa" to datada or "" and "dataAdozioneA" to dataa or ""
    - buildpageparams(anno, keyword, page, datada=None, dataa=None): sets "DATAADOZIONEDA" to datada or "" and "DATAADOZIONEA" to dataa or ""
    - fetchfirstpage(session, anno, keyword, timeout, datada=None, dataa=None): passes datada/dataa to buildpayload
    - fetchpage(session, anno, keyword, page, timeout, datada=None, dataa=None): passes datada/dataa to fetchfirstpage (page==1) and buildpageparams (page>1)
    - When datada=None and dataa=None the payload dicts are identical to current output (empty strings for both date keys) — backward compatible
    - When datada="04/06/2026", dataa="11/06/2026" the payload carries those values verbatim
  </behavior>
  <action>
    Modify the four functions in delibere.py in this order:

    1. buildpayload: add datada=None, dataa=None after keyword. Replace the two empty-string literals
       with datada or "" and dataa or "" respectively. The keys remain "dataAdozioneDa" and "dataAdozioneA".

    2. buildpageparams: add datada=None, dataa=None after page. Replace "DATAADOZIONEDA": "" with
       datada or "" and "DATAADOZIONEA": "" with dataa or "". Note the portal spells the end-date key
       "DATAADOZIONEA" (no second D).

    3. fetchfirstpage: add datada=None, dataa=None after timeout. Forward both to buildpayload call.

    4. fetchpage: add datada=None, dataa=None after timeout. Forward to fetchfirstpage (page==1 branch)
       and to buildpageparams (page>1 branch). The session.get call already passes params=buildpageparams(...),
       so add the two kwargs there.

    No other functions or call sites change in this task — scraperesults still calls fetchpage without
    date args (that wiring happens in Task 2).
  </action>
  <verify>
    <automated>python -c "
import delibere
p = delibere.buildpayload('2026', 'FESR')
assert p['dataAdozioneDa'] == '' and p['dataAdozioneA'] == '', 'None defaults broken'
p2 = delibere.buildpayload('2026', 'FESR', datada='04/06/2026', dataa='11/06/2026')
assert p2['dataAdozioneDa'] == '04/06/2026', f'datada not wired: {p2}'
assert p2['dataAdozioneA'] == '11/06/2026', f'dataa not wired: {p2}'
q = delibere.buildpageparams('2026', 'FESR', 2)
assert q['DATAADOZIONEDA'] == '' and q['DATAADOZIONEA'] == '', 'pageparams None defaults broken'
q2 = delibere.buildpageparams('2026', 'FESR', 2, datada='04/06/2026', dataa='11/06/2026')
assert q2['DATAADOZIONEDA'] == '04/06/2026', f'pageparams datada not wired: {q2}'
assert q2['DATAADOZIONEA'] == '11/06/2026', f'pageparams dataa not wired: {q2}'
print('Task 1 OK')
"
    </automated>
  </verify>
  <done>
    buildpayload, buildpageparams, fetchfirstpage, fetchpage all accept datada/dataa kwargs.
    When both are None the dict output is identical to before. When provided, the correct portal
    keys carry the date strings. Python import check passes with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add datada/dataa to scraperesults with DD/MM/YYYY validation; extend writesummaries with date_suffix</name>
  <files>delibere.py</files>
  <read_first>
    delibere.py lines 258-348 — current scraperesults and writesummaries bodies, including
    the fetchpage call at the start of the while loop and the suffix computation in writesummaries.
  </read_first>
  <behavior>
    scraperesults validation:
    - datada=None and dataa=None: no validation, passes straight through (DATE-02 compliance)
    - datada="abc": raises ValueError with a message containing "DD/MM/YYYY"
    - datada="2026-06-04": raises ValueError (wrong format — portal expects DD/MM/YYYY)
    - datada="04/06/2026": passes validation
    - dataa="11/06/2026": passes validation
    - Validation runs before the session is created or any HTTP call is made

    scraperesults forwarding:
    - fetchpage call inside the while loop gains datada=datada, dataa=dataa kwargs
    - Both None passed through unchanged (empty strings land in payload — same as current)

    writesummaries extension:
    - New optional param: date_suffix=None (added after folder)
    - When date_suffix is not None/empty, the four CSV filenames use date_suffix instead of
      the locally computed suffix: f"riepilogo_azioni_{date_suffix}.csv" etc.
    - When date_suffix is None the function is backward compatible (computes suffix itself as before)
    - scraper.py will pass date_suffix in Plan 02; existing calls without it continue to work
  </behavior>
  <action>
    Modify scraperesults in delibere.py:

    1. Add datada=None, dataa=None after outputfolder in the signature (keep existing defaults
       for all existing params unchanged).

    2. Immediately after the docstring (if any) or at the very start of the function body, before
       session = requests.Session(), add a validation block:
       - Import datetime at the top of the file if not already imported (check existing imports first).
       - For each of datada, dataa: if the value is not None, attempt
         datetime.strptime(value, "%d/%m/%Y"). On ValueError re-raise with a message that includes
         "DD/MM/YYYY" and the bad value.

    3. In the while loop, change the existing fetchpage call to pass datada=datada, dataa=dataa.

    Modify writesummaries in delibere.py:

    4. Add date_suffix=None after folder in the signature.

    5. Replace the suffix computation line with:
         suffix = date_suffix if date_suffix else f"{anno}_{keyword.lower()}".replace(" ", "_")
       The four writesummary calls already use suffix — no further changes needed there.

    Do NOT modify any other function. datetime is in the standard library — no new dependency.
    Do NOT import datetime if it is already imported (check the import block at the top of the file).
  </action>
  <verify>
    <automated>python -c "
import delibere, inspect

# Signature check
sig = inspect.signature(delibere.scraperesults)
params = list(sig.parameters)
assert 'datada' in params, 'datada missing from scraperesults'
assert 'dataa' in params, 'dataa missing from scraperesults'
assert sig.parameters['datada'].default is None, 'datada default not None'
assert sig.parameters['dataa'].default is None, 'dataa default not None'

# Validation: bad format raises ValueError
try:
    delibere.scraperesults.__wrapped__ if hasattr(delibere.scraperesults, '__wrapped__') else None
except:
    pass

import unittest.mock as mock

# Patch fetchpage so no HTTP is made
with mock.patch('delibere.fetchpage') as mfp:
    mfp.return_value = '<html></html>'
    try:
        delibere.scraperesults('2026', 'FESR', datada='not-a-date', maxpages=0)
        assert False, 'Should have raised ValueError'
    except ValueError as e:
        assert 'DD/MM/YYYY' in str(e), f'ValueError message missing DD/MM/YYYY: {e}'

# writesummaries date_suffix param exists
ws_sig = inspect.signature(delibere.writesummaries)
assert 'date_suffix' in ws_sig.parameters, 'date_suffix missing from writesummaries'
assert ws_sig.parameters['date_suffix'].default is None, 'date_suffix default not None'

print('Task 2 OK')
"
    </automated>
  </verify>
  <done>
    scraperesults accepts datada and dataa (both default None). Passing a non-DD/MM/YYYY string
    raises ValueError before any network call. Both None passes validation and behavior is unchanged.
    writesummaries accepts date_suffix=None; when provided it overrides the computed suffix.
    All existing call sites without new params continue to work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper.py config -> delibere.py | Date strings from config vars cross into validation logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | buildpayload/buildpageparams date fields | mitigate | Validate DD/MM/YYYY format with datetime.strptime in scraperesults before reaching HTTP layer; invalid values raise ValueError and halt execution |
| T-01-02 | Information Disclosure | Portal HTTP responses | accept | Responses contain only public delibere data; no PII or secrets in scope for this phase |
| T-01-SC | Tampering | No new packages installed in this plan | accept | Pure stdlib (datetime) addition; no package manager invocations |
</threat_model>

<verification>
Run the automated checks in each task's `<verify>` block in sequence.
Both must print "OK" with no assertion errors.

Additionally verify backward compatibility: run the Task 1 and Task 2 checks with all new
params omitted (simulating existing call sites) to confirm no TypeError is raised.
</verification>

<success_criteria>
- delibere.buildpayload("2026", "FESR") returns dict with "dataAdozioneDa": "" (unchanged behavior)
- delibere.buildpayload("2026", "FESR", datada="04/06/2026") returns dict with "dataAdozioneDa": "04/06/2026"
- delibere.scraperesults called with datada="bad" raises ValueError containing "DD/MM/YYYY"
- delibere.scraperesults called with datada=None, dataa=None passes validation silently
- delibere.writesummaries signature includes date_suffix=None as last param
</success_criteria>

<output>
Create .planning/phases/01-date-filtering/01-01-SUMMARY.md when done.
</output>
