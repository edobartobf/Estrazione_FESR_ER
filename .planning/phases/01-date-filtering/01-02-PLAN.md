---
phase: 01-date-filtering
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scraper.py
autonomous: true
requirements:
  - DATE-03

must_haves:
  truths:
    - "Setting DATA_DA and DATA_A in scraper.py causes output filenames to include the date range tokens"
    - "Filename tokens use Italian abbreviated month names (gen/feb/mar/apr/mag/giu/lug/ago/set/ott/nov/dic)"
    - "When DATA_DA = None and DATA_A = None filenames are identical to current behavior"
    - "Summary CSVs also carry the date suffix when filter is active"
    - "The date suffix appears for each per-year file and for the combined multi-year file"
  artifacts:
    - path: "scraper.py"
      provides: "Config vars DATA_DA/DATA_A, date suffix computation, date args forwarded to scraperesults/writesummaries"
      contains: "DATA_DA"
  key_links:
    - from: "scraper.py config"
      to: "scraper.py:main() suffix computation"
      via: "DATA_DA / DATA_A consumed by datesuffix() helper"
      pattern: "datesuffix"
    - from: "scraper.py:main()"
      to: "delibere.scraperesults()"
      via: "datada=DATA_DA, dataa=DATA_A kwargs"
      pattern: "scraperesults.*datada"
    - from: "scraper.py:main()"
      to: "delibere.writesummaries()"
      via: "date_suffix= kwarg"
      pattern: "writesummaries.*date_suffix"
---

<objective>
Wire the date-range config vars into scraper.py and implement Italian-month filename suffixes.

Purpose: Plan 01 exposed filtering in delibere.py. This plan adds the user-facing config knobs
(DATA_DA / DATA_A), computes the Italian abbreviated-month date tokens (e.g., "04giu"), builds
the full date-aware filename suffix, and passes it to scraperesults() and writesummaries() so
every output file reflects the active date range.

Output: scraper.py with DATA_DA/DATA_A config, a datesuffix() helper, and main() updated to
produce filenames like delibere_2026_04giu_11giu_fesr.csv when filtering is active.
</objective>

<execution_context>
@C:\Users\edoba\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edoba\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-date-filtering/01-CONTEXT.md
@.planning/phases/01-date-filtering/01-01-SUMMARY.md

<interfaces>
<!-- Signatures after Plan 01 is applied. Executor uses these; no file re-read needed. -->

From delibere.py (after 01-01-PLAN.md):
  scraperesults(anno, keyword, maxpages=None, delay=0.4, includelinks=False,
                download=False, maxpdf=None, timeout=30, outputfolder="data",
                datada=None, dataa=None) -> list[dict]

  writesummaries(records, anno, keyword, folder, date_suffix=None) -> None

Current scraper.py config block (lines 47-57):
  ANNI = "2026"
  KEYWORD = "FESR"
  CARTELLA_OUTPUT = "data"
  SCARICA_LINK_PDF = False
  SCARICA_PDF = False
  MAX_PAGINE = None
  MAX_PDF = None
  PAUSA_SECONDI = 0.4
  TIMEOUT_SECONDI = 30

Current main() suffix computation (line 86):
  suffix = f"{anno}_{KEYWORD.lower()}".replace(" ", "_")

Current scraperesults call (lines 88-98):
  records = scraperesults(anno=anno, keyword=KEYWORD, maxpages=MAX_PAGINE, ...)

Current writesummaries call (line 105):
  writesummaries(records, anno, KEYWORD, folder)

Current multi-year suffix (line 114):
  suffix = f"{anni[0]}_{anni[-1]}_{KEYWORD.lower()}".replace(" ", "_")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DATA_DA / DATA_A config vars and datesuffix() helper to scraper.py</name>
  <files>scraper.py</files>
  <read_first>
    scraper.py lines 1-60 — imports, config comment block, and all existing config vars.
    Confirm exact line numbers of the config section end and the leggianni() function start.
  </read_first>
  <behavior>
    Config vars (inserted after TIMEOUT_SECONDI = 30, before the blank line that precedes leggianni()):
    - DATA_DA = None  # Esempio: "04/06/2026"
    - DATA_A = None   # Esempio: "11/06/2026"

    datesuffix(datada, dataa) helper function — inserted between the config block and leggianni()
    or between leggianni() and main(), either is fine:
    - MESI = ["gen","feb","mar","apr","mag","giu","lug","ago","set","ott","nov","dic"]
    - If datada is None and dataa is None: return ""
    - If only datada is set: parse DD/MM/YYYY -> return f"{day:02d}{MESI[month-1]}"  (e.g. "04giu")
    - If only dataa is set: same token for dataa
    - If both are set: return f"{token_da}_{token_a}"  (e.g. "04giu_11giu")
    - Parsing uses datetime.strptime(value, "%d/%m/%Y") — already validated by scraperesults(),
      so here it can raise without extra error handling
    - Import datetime at top of scraper.py if not already present

    The config comment block (the large "MODIFICA SOLO QUESTA SEZIONE" block) must be updated
    to document DATA_DA / DATA_A with example values. Add the following example entry to the
    comment block (anywhere logical after the existing examples):
      # - Filtrare per periodo (una settimana):
      #   DATA_DA = "04/06/2026"
      #   DATA_A  = "11/06/2026"
  </behavior>
  <action>
    1. At the top of scraper.py, after the existing imports (line 1-2), add:
         from datetime import datetime
       Only add this if datetime is not already imported. Check the current import block first.

    2. In the config section, after the line TIMEOUT_SECONDI = 30, add two new lines:
         DATA_DA = None  # Esempio: "04/06/2026"
         DATA_A  = None  # Esempio: "11/06/2026"

    3. Add the example entry to the large comment block. Insert it before the closing
       "# ============================================================" line.

    4. Define datesuffix(datada, dataa) as a module-level function. Place it after leggianni()
       and before main(). The function body:
       - Define MESI as a local list of 12 three-letter Italian month abbreviations in order.
       - Define an inner helper _tok(d) that parses a DD/MM/YYYY string and returns
         the zero-padded day concatenated with the matching MESI entry (e.g. "04giu").
       - Return "" if both datada and dataa are None.
       - If datada is not None and dataa is None: return _tok(datada)
       - If datada is None and dataa is not None: return _tok(dataa)
       - If both are not None: return f"{_tok(datada)}_{_tok(dataa)}"

    Do NOT use fenced code blocks in the action. Reference identifiers by name only.
    The datetime import uses strptime which requires the datetime class, so import via
    "from datetime import datetime" (strptime is a classmethod, not a module-level function).
  </action>
  <verify>
    <automated>python -c "
import scraper, inspect

# Config vars exist and default to None
assert hasattr(scraper, 'DATA_DA'), 'DATA_DA missing'
assert hasattr(scraper, 'DATA_A'), 'DATA_A missing'
assert scraper.DATA_DA is None, 'DATA_DA default not None'
assert scraper.DATA_A is None, 'DATA_A default not None'

# datesuffix function exists
assert hasattr(scraper, 'datesuffix'), 'datesuffix() missing'

# Both None -> empty string
assert scraper.datesuffix(None, None) == '', f'Expected empty string: {scraper.datesuffix(None, None)}'

# Single date
assert scraper.datesuffix('04/06/2026', None) == '04giu', f'Single datada: {scraper.datesuffix(\"04/06/2026\", None)}'
assert scraper.datesuffix(None, '11/06/2026') == '11giu', f'Single dataa: {scraper.datesuffix(None, \"11/06/2026\")}'

# Both dates
assert scraper.datesuffix('04/06/2026', '11/06/2026') == '04giu_11giu', f'Both: {scraper.datesuffix(\"04/06/2026\", \"11/06/2026\")}'

# Other months
assert scraper.datesuffix('01/01/2026', None) == '01gen'
assert scraper.datesuffix('15/12/2025', None) == '15dic'
assert scraper.datesuffix('03/04/2026', '10/04/2026') == '03apr_10apr'

print('Task 1 OK')
"
    </automated>
  </verify>
  <done>
    DATA_DA and DATA_A config vars exist in scraper.py with None defaults.
    datesuffix() returns correct Italian-month tokens for all combinations.
    Comment block documents the new date filter usage example.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire date suffix into main() filenames and forward dates to scraperesults/writesummaries</name>
  <files>scraper.py</files>
  <read_first>
    scraper.py lines 78-131 — full main() function, including the per-year loop, suffix computation,
    scraperesults call, writecsv/writejson/writesummaries calls, and the multi-year block.
  </read_first>
  <behavior>
    Per-year loop (inside "for anno in anni:"):
    - Compute ds = datesuffix(DATA_DA, DATA_A)
    - When ds is truthy: suffix = f"{anno}_{ds}_{KEYWORD.lower()}".replace(" ", "_")
    - When ds is falsy: suffix = f"{anno}_{KEYWORD.lower()}".replace(" ", "_")  (unchanged)
    - scraperesults call gains: datada=DATA_DA, dataa=DATA_A
    - writesummaries call gains: date_suffix=f"{anno}_{ds}" if ds else None
      (so summary filenames become riepilogo_azioni_2026_04giu_11giu_fesr.csv)
      Actually: pass date_suffix=suffix so summaries use the same suffix as main CSV

    Multi-year block (the "if len(anni) > 1:" section):
    - Compute ds the same way (ds = datesuffix(DATA_DA, DATA_A), already computed outside the loop
      or recompute — either is fine, just consistent)
    - When ds is truthy: suffix = f"{anni[0]}_{anni[-1]}_{ds}_{KEYWORD.lower()}".replace(" ", "_")
    - When ds is falsy: current behavior unchanged
    - writesummaries call in the multi-year block also gains date_suffix=suffix

    The _tutti_gli_anni filenames retain that literal string:
    e.g. delibere_2023_2026_04giu_11giu_fesr_tutti_gli_anni.csv

    Print statements in main() do NOT need changes — they print the actual path variables which
    already carry the updated suffix.
  </behavior>
  <action>
    Edit main() in scraper.py. Do NOT restructure the function — surgical edits only:

    1. Before the "for anno in anni:" loop, add:
         ds = datesuffix(DATA_DA, DATA_A)

    2. Inside the loop, replace the existing suffix = f"{anno}_{KEYWORD.lower()}..." line with
       a conditional that checks ds and builds the appropriate suffix string (per <behavior>).

    3. Add datada=DATA_DA, dataa=DATA_A to the existing scraperesults() call (append after the
       last existing keyword arg, outputfolder=CARTELLA_OUTPUT).

    4. Change the writesummaries() call to pass date_suffix=suffix as the fifth positional/keyword arg
       (the existing four args remain unchanged: records, anno, KEYWORD, folder).

    5. In the "if len(anni) > 1:" block, replace the existing suffix computation with a conditional
       that inserts ds between the year range and the keyword when ds is truthy.

    6. Change the writesummaries() call in the multi-year block the same way (add date_suffix=suffix).

    Refer to datesuffix and DATA_DA/DATA_A by their exact names. No new imports needed — all
    required names are now module-level in scraper.py.
  </action>
  <verify>
    <automated>python -c "
import scraper, unittest.mock as mock
from pathlib import Path

# Test: with DATA_DA and DATA_A set, filename suffix includes date tokens
scraper.DATA_DA = '04/06/2026'
scraper.DATA_A = '11/06/2026'
scraper.ANNI = '2026'
scraper.KEYWORD = 'FESR'
scraper.MAX_PAGINE = 0  # don't actually fetch

written_paths = []

def fake_scraperesults(**kwargs):
    assert kwargs.get('datada') == '04/06/2026', f'datada not forwarded: {kwargs}'
    assert kwargs.get('dataa') == '11/06/2026', f'dataa not forwarded: {kwargs}'
    return []

def fake_writecsv(path, records, *args, **kwargs):
    written_paths.append(str(path))

def fake_writejson(path, records):
    written_paths.append(str(path))

def fake_writesummaries(records, anno, keyword, folder, date_suffix=None):
    assert date_suffix is not None and '04giu' in date_suffix, f'date_suffix wrong: {date_suffix}'
    assert '11giu' in date_suffix, f'date_suffix missing dataa: {date_suffix}'

with mock.patch('scraper.scraperesults', side_effect=lambda *a, **kw: fake_scraperesults(**kw) or []):
    with mock.patch('scraper.writecsv', fake_writecsv):
        with mock.patch('scraper.writejson', fake_writejson):
            with mock.patch('scraper.writesummaries', fake_writesummaries):
                scraper.main()

# At least one CSV path should contain '04giu' and '11giu'
date_paths = [p for p in written_paths if '04giu' in p and '11giu' in p]
assert date_paths, f'No date tokens in output paths: {written_paths}'

# Test: with None, filenames unchanged
scraper.DATA_DA = None
scraper.DATA_A = None
written_paths.clear()

def fake_writesummaries_nodates(records, anno, keyword, folder, date_suffix=None):
    assert date_suffix is None or ('giu' not in (date_suffix or '')), f'Unexpected date_suffix: {date_suffix}'

with mock.patch('scraper.scraperesults', return_value=[]):
    with mock.patch('scraper.writecsv', fake_writecsv):
        with mock.patch('scraper.writejson', fake_writejson):
            with mock.patch('scraper.writesummaries', fake_writesummaries_nodates):
                scraper.main()

no_date_paths = [p for p in written_paths if 'giu' not in p]
assert no_date_paths, f'Paths should not contain month tokens when no date set: {written_paths}'

print('Task 2 OK')
"
    </automated>
  </verify>
  <done>
    With DATA_DA="04/06/2026" and DATA_A="11/06/2026", scraper.main() produces files named
    delibere_2026_04giu_11giu_fesr.csv and passes date_suffix containing "04giu_11giu" to
    writesummaries. With both None, filename is delibere_2026_fesr.csv (unchanged).
    datada and dataa forwarded correctly to scraperesults().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper.py config vars -> datesuffix() | User-supplied date strings enter the date parsing helper |
| datesuffix() tokens -> filesystem paths | Computed tokens become part of output file names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | datesuffix() date parsing | accept | Input already validated by scraperesults() before reaching filesystem. datesuffix() is called after validation; strptime failure here is a programmer error, not an attack surface |
| T-02-02 | Tampering | Filename construction with date tokens | accept | Tokens derived from parsed datetime object — no raw string concatenation of user input into paths. Output folder is a local directory under user control |
| T-02-SC | Tampering | No new packages in this plan | accept | Only stdlib additions (from datetime import datetime); no package manager invocations |
</threat_model>

<verification>
Run the automated checks in each task's `<verify>` block in sequence.
Both must print "OK" with no assertion errors.

End-to-end filename check (manual, optional):
  Set DATA_DA = "04/06/2026", DATA_A = "11/06/2026", MAX_PAGINE = 1, SCARICA_PDF = False
  in scraper.py and run: python scraper.py
  Verify that data/delibere_2026_04giu_11giu_fesr.csv is created.
</verification>

<success_criteria>
- scraper.DATA_DA and scraper.DATA_A exist, both default to None
- scraper.datesuffix("04/06/2026", "11/06/2026") returns "04giu_11giu"
- scraper.datesuffix(None, None) returns ""
- With DATA_DA/DATA_A set, CSV output path contains both date tokens
- With DATA_DA/DATA_A as None, CSV path is identical to pre-phase behavior
- writesummaries called with date_suffix carrying the date tokens when filter is active
</success_criteria>

<output>
Create .planning/phases/01-date-filtering/01-02-SUMMARY.md when done.
</output>
