---
phase: 05-drive-subfolder-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - upload_drive.py
  - test_upload_drive_subfolder.py
autonomous: true
requirements:
  - DRIVE-01
  - DRIVE-02

must_haves:
  truths:
    - "Ogni run crea una nuova sottocartella in GDRIVE_FOLDER_ID con nome FESR_{datesuffix} (es. FESR_04giu_10giu)"
    - "Quando FESR_DATA_DA e FESR_DATA_A non sono impostati, il nome cade su FESR_{YYYYMMDD} con la data odierna"
    - "Tutti i file in data/ (CSV + PDF) vengono caricati nella sottocartella, non flat in GDRIVE_FOLDER_ID"
    - "drive_url.txt contiene il link alla sottocartella (https://drive.google.com/drive/folders/{subfolder_id}), non alla root"
    - "Run successivi creano nuove sottocartelle separate — nessuna sovrascrittura"
  artifacts:
    - path: "upload_drive.py"
      provides: "create_subfolder(), datesuffix(), upload_to_subfolder(), main() aggiornato"
      min_lines: 80
    - path: "test_upload_drive_subfolder.py"
      provides: "Test unitari per datesuffix() e logica nome cartella"
      exports: []
  key_links:
    - from: "upload_drive.py:main()"
      to: "upload_drive.py:create_subfolder()"
      via: "service passato da main dopo build_credentials"
      pattern: "create_subfolder\\(service,"
    - from: "upload_drive.py:main()"
      to: "drive_url.txt"
      via: "Path('drive_url.txt').write_text"
      pattern: "drive\\.google\\.com/drive/folders/\\{subfolder_id\\}"
    - from: "upload_drive.py:create_subfolder()"
      to: "Drive API files.create"
      via: "service.files().create(body={mimeType: application/vnd.google-apps.folder})"
      pattern: "application/vnd\\.google-apps\\.folder"
---

<objective>
Refactoring di upload_drive.py per creare una sottocartella datata in Drive ad ogni run e caricarvi tutti i file (PDF + CSV). La cartella root GDRIVE_FOLDER_ID diventa un contenitore organizzato per settimana.

Purpose: Soddisfa DRIVE-01 (crea sottocartella con nome periodo) e DRIVE-02 (upload nella sottocartella; drive_url.txt punta alla sottocartella).
Output: upload_drive.py riscritto con create_subfolder(), datesuffix() standalone, upload_to_subfolder(); test unitari per la logica di naming.
</objective>

<execution_context>
@C:/Users/edoba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edoba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-drive-subfolder-upload/05-CONTEXT.md

<interfaces>
<!-- Estratto da upload_drive.py attuale (68 righe) — leggere prima di modificare -->

Funzioni esistenti da rinominare / refactoring:

```python
# ATTUALE — da rinominare e modificare
def upload_folder(folder_path, folder_id, creds):
    service = build("drive", "v3", credentials=creds)   # service costruito internamente
    # ... rglob loop ...

def main():
    creds = build_credentials(client_id, client_secret, refresh_token)
    uploaded = upload_folder(output_folder, folder_id, creds)
    folder_url = f"https://drive.google.com/drive/folders/{folder_id}"  # punta alla root
    Path("drive_url.txt").write_text(folder_url, encoding="utf-8")
```

Pattern datesuffix() da scraper.py (righe 117-130) — replicare inline, NON importare:

```python
def datesuffix(datada, dataa):
    MESI = ["gen","feb","mar","apr","mag","giu","lug","ago","set","ott","nov","dic"]
    def _tok(d):
        dt = datetime.strptime(d, "%d/%m/%Y")
        return f"{dt.day:02d}{MESI[dt.month - 1]}"
    if datada is None and dataa is None:
        return ""
    if datada is not None and dataa is None:
        return _tok(datada)
    if datada is None and dataa is not None:
        return _tok(dataa)
    return f"{_tok(datada)}_{_tok(dataa)}"
```

Env var disponibili nel passo "Carica su Drive" del workflow (via GITHUB_ENV dal passo "Calcola periodo"):
- FESR_DATA_DA  — es. "04/06/2026"
- FESR_DATA_A   — es. "10/06/2026"
- GDRIVE_FOLDER_ID — ID cartella root Drive
- GDRIVE_CLIENT_ID, GDRIVE_CLIENT_SECRET, GDRIVE_REFRESH_TOKEN — OAuth2
- FESR_OUTPUT   — cartella output locale (default "data")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggiungi datesuffix() e logica nome cartella con test TDD</name>
  <files>test_upload_drive_subfolder.py, upload_drive.py</files>
  <behavior>
    - datesuffix("04/06/2026", "10/06/2026") -> "04giu_10giu"
    - datesuffix("04/06/2026", None)          -> "04giu"
    - datesuffix(None, "10/06/2026")          -> "10giu"
    - datesuffix(None, None)                  -> ""
    - nome_cartella("04/06/2026", "10/06/2026") -> "FESR_04giu_10giu"
    - nome_cartella(None, None) con oggi=date(2026,6,11) -> "FESR_20260611"
    - nome_cartella("04/06/2026", None)        -> "FESR_04giu"
  </behavior>
  <action>
RED: Crea test_upload_drive_subfolder.py con i 7 casi sopra. Importa datesuffix e una funzione helper nome_cartella da upload_drive (non ancora esistenti). Esegui i test — devono fallire con ImportError o AttributeError.

Commit: test(05-01): add failing tests for datesuffix and folder naming

GREEN: In upload_drive.py aggiungi:

1. Import datetime.date all'inizio (datetime già importato da scraper — aggiungere: from datetime import datetime, date).

2. Funzione datesuffix(datada, dataa) — copia esatta della logica di scraper.py righe 117-130 (lista MESI inline, _tok() nested, quattro rami if). Nessun import da scraper.py.

3. Funzione nome_cartella(datada, dataa) — logica:
   - Se datesuffix restituisce stringa non vuota: return f"FESR_{datesuffix(datada, dataa)}"
   - Altrimenti fallback: return f"FESR_{date.today().strftime('%Y%m%d')}"

Esegui i test — devono passare tutti.

Commit: feat(05-01): add datesuffix() and nome_cartella() to upload_drive.py
  </action>
  <verify>
    <automated>python -m pytest test_upload_drive_subfolder.py -v</automated>
  </verify>
  <done>7 test passano. upload_drive.py contiene datesuffix() e nome_cartella() standalone senza import da scraper.py.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiungi create_subfolder() e refactoring upload_folder() → upload_to_subfolder()</name>
  <files>upload_drive.py</files>
  <action>
Modifica upload_drive.py apportando queste tre modifiche distinte:

1. NUOVA FUNZIONE create_subfolder(service, parent_id, name):
   - Chiama service.files().create() con body={"name": name, "mimeType": "application/vnd.google-apps.folder", "parents": [parent_id]} e fields="id"
   - Restituisce l'ID della sottocartella creata (result["id"])
   - Stampa a stdout: f"Sottocartella creata: {name}"

2. RENAME upload_folder() → upload_to_subfolder(folder_path, subfolder_id, service):
   - Firma: (folder_path, subfolder_id, service) — service al posto di creds, NON ricostruire il service internamente
   - Rimuovi la riga: service = build("drive", "v3", credentials=creds)
   - Sostituisci folder_id con subfolder_id nel metadata parents
   - Il loop rglob e il resto della logica rimangono invariati

3. AGGIORNA main():
   a. Dopo build_credentials(), chiama service = build("drive", "v3", credentials=creds)
   b. Leggi datada = os.environ.get("FESR_DATA_DA") e dataa = os.environ.get("FESR_DATA_A")
   c. Calcola cartella_nome = nome_cartella(datada, dataa)
   d. Chiama subfolder_id = create_subfolder(service, folder_id, cartella_nome)
   e. Chiama uploaded = upload_to_subfolder(output_folder, subfolder_id, service)
   f. Costruisci subfolder_url = f"https://drive.google.com/drive/folders/{subfolder_id}"
   g. Stampa f"Caricati {len(uploaded)} file su Drive: {subfolder_url}"
   h. Scrivi drive_url.txt con subfolder_url (non folder_id root)

Nessuna modifica a build_credentials(), SCOPES, o agli import esistenti — aggiungi solo "date" a "from datetime import datetime" se non già presente dal Task 1.
  </action>
  <verify>
    <automated>python -c "
import ast, sys
src = open('upload_drive.py').read()
tree = ast.parse(src)
fns = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'create_subfolder' in fns, 'create_subfolder mancante'
assert 'upload_to_subfolder' in fns, 'upload_to_subfolder mancante'
assert 'upload_folder' not in fns, 'upload_folder (vecchio nome) ancora presente'
assert 'nome_cartella' in fns, 'nome_cartella mancante'
assert 'datesuffix' in fns, 'datesuffix mancante'
# Verifica che upload_to_subfolder non ricostruisca service internamente
import inspect
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef) and node.name == 'upload_to_subfolder':
        src_fn = ast.get_source_segment(src, node)
        assert 'build(' not in src_fn, 'upload_to_subfolder non deve chiamare build()'
print('OK: tutte le funzioni presenti e corrette')
"
python -m pytest test_upload_drive_subfolder.py -v
</automated>
  </verify>
  <done>
- upload_drive.py ha 5 funzioni: build_credentials, datesuffix, nome_cartella, create_subfolder, upload_to_subfolder, main
- upload_folder non esiste più
- upload_to_subfolder non chiama build() internamente
- main() crea la sottocartella, carica i file nella sottocartella, scrive il link sottocartella in drive_url.txt
- Tutti i test del Task 1 ancora passanti
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GITHUB_ENV → upload_drive.py | FESR_DATA_DA / FESR_DATA_A letti da env — iniettati da step "Calcola periodo" nel runner |
| upload_drive.py → Drive API | Credenziali OAuth2 da GitHub Secrets — token non loggato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | subfolder name via FESR_DATA_DA/A | accept | I valori provengono da GITHUB_ENV impostato da step precedente nello stesso job — non da input utente diretto in questo contesto; runner isolato |
| T-05-02 | Information Disclosure | drive_url.txt in artifact Actions | accept | Il file contiene solo un URL pubblico Drive; nessun segreto. La cartella Drive stessa è protetta da ACL Drive |
| T-05-03 | Spoofing | OAuth2 refresh token | mitigate | Token in GitHub Secrets, mai loggato. build_credentials() usa google-auth — nessuna gestione manuale del token |
| T-05-04 | Denial of Service | Creazione illimitata di sottocartelle | accept | Ogni run crea una sola sottocartella; frequenza limitata dal cron settimanale e dal rate limit Drive API |
| T-05-SC | Tampering | Dipendenze pip (google-api-python-client et al.) | mitigate | Dipendenze già presenti e validate in v1.0 Phase 3 — nessun nuovo pacchetto introdotto in questo piano |
</threat_model>

<verification>
Test suite completa:

```
python -m pytest test_upload_drive_subfolder.py -v
```

Verifica struttura statica del modulo aggiornato:

```
python -c "
import ast
src = open('upload_drive.py').read()
tree = ast.parse(src)
fns = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
print('Funzioni:', fns)
assert set(['build_credentials','datesuffix','nome_cartella','create_subfolder','upload_to_subfolder','main']).issubset(set(fns))
assert 'upload_folder' not in fns
print('Struttura OK')
"
```

Verifica manuale del flusso completo (opzionale, richiede credenziali Drive reali):

```
GDRIVE_CLIENT_ID=... GDRIVE_CLIENT_SECRET=... GDRIVE_REFRESH_TOKEN=... \
GDRIVE_FOLDER_ID=... FESR_DATA_DA="04/06/2026" FESR_DATA_A="10/06/2026" \
FESR_OUTPUT=data python upload_drive.py
```

Risultato atteso: sottocartella "FESR_04giu_10giu" visibile in GDRIVE_FOLDER_ID; drive_url.txt contiene l'URL della sottocartella.
</verification>

<success_criteria>
1. python -m pytest test_upload_drive_subfolder.py -v — tutti i test passano (0 failed)
2. upload_drive.py parsable da ast.parse senza errori
3. upload_folder non esiste più in upload_drive.py
4. create_subfolder, upload_to_subfolder, datesuffix, nome_cartella presenti
5. upload_to_subfolder non contiene chiamate a build()
6. main() scrive drive_url.txt con URL che include subfolder_id (non folder_id root) — verificabile a ispezione statica: Path("drive_url.txt").write_text(subfolder_url nel codice, non folder_url con folder_id)
7. Nessun import da scraper.py in upload_drive.py
</success_criteria>

<output>
Quando tutti i task sono completati, crea `.planning/phases/05-drive-subfolder-upload/05-01-SUMMARY.md` seguendo il template in @C:/Users/edoba/.claude/get-shit-done/templates/summary.md.

Includi nel summary:
- requirements-completed: [DRIVE-01, DRIVE-02]
- key-files.created: [test_upload_drive_subfolder.py]
- key-files.modified: [upload_drive.py]
- patterns-established: ["Subfolder Drive creata via files.create con mimeType application/vnd.google-apps.folder", "datesuffix() duplicato inline in upload_drive.py — nessun import cross-script", "Service OAuth2 costruito una volta in main() e passato alle funzioni"]
</output>
