---
phase: "04-pdf-download"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scraper.py
autonomous: true
requirements:
  - PDF-01

must_haves:
  truths:
    - "Se FESR_SCARICA_LINK_PDF=true, il modulo SCARICA_LINK_PDF diventa True dopo applysecrets()"
    - "Se FESR_SCARICA_PDF=true, il modulo SCARICA_PDF diventa True dopo applysecrets()"
    - "Se solo FESR_SCARICA_PDF=true (senza FESR_SCARICA_LINK_PDF), entrambe le variabili diventano True (implicazione prerequisito)"
    - "Se le env var non sono impostate o sono vuote, i valori rimangono False (comportamento invariato)"
  artifacts:
    - path: "scraper.py"
      provides: "Override SCARICA_LINK_PDF e SCARICA_PDF tramite env var FESR_*"
      contains: "FESR_SCARICA_LINK_PDF"
  key_links:
    - from: "env FESR_SCARICA_PDF"
      to: "scraper.py:SCARICA_PDF"
      via: "applysecrets()"
      pattern: "FESR_SCARICA_PDF"
---

<objective>
Aggiunge il parsing delle env var `FESR_SCARICA_LINK_PDF` e `FESR_SCARICA_PDF` all'interno di `applysecrets()` in `scraper.py`, rendendo configurabili dall'esterno le variabili globali `SCARICA_LINK_PDF` e `SCARICA_PDF` (attualmente hardcoded a `False`).

Purpose: Prerequisito per abilitare il download PDF dal workflow GitHub Actions senza modifiche manuali al codice.
Output: `scraper.py` con le due env var gestite in `applysecrets()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Esporre FESR_SCARICA_LINK_PDF e FESR_SCARICA_PDF in applysecrets()</name>
  <files>scraper.py</files>

  <read_first>
    - scraper.py (righe 55-87): variabili globali SCARICA_LINK_PDF / SCARICA_PDF e corpo completo di applysecrets()
  </read_first>

  <behavior>
    - Test 1: os.environ["FESR_SCARICA_LINK_PDF"] = "true" → dopo applysecrets(), SCARICA_LINK_PDF is True
    - Test 2: os.environ["FESR_SCARICA_PDF"] = "1" → dopo applysecrets(), SCARICA_PDF is True
    - Test 3: os.environ["FESR_SCARICA_PDF"] = "true", FESR_SCARICA_LINK_PDF non impostata → entrambe True
    - Test 4: env var non impostata → SCARICA_LINK_PDF e SCARICA_PDF rimangono False
    - Test 5: os.environ["FESR_SCARICA_LINK_PDF"] = "yes" → SCARICA_LINK_PDF is True
    - Test 6: os.environ["FESR_SCARICA_LINK_PDF"] = "false" → SCARICA_LINK_PDF rimane False
  </behavior>

  <action>
    Modifica scraper.py in due punti:

    1. Riga 71 — aggiungi SCARICA_LINK_PDF e SCARICA_PDF alla dichiarazione global:
       `global KEYWORD, DATA_DA, DATA_A, ANNI, CARTELLA_OUTPUT, SCARICA_LINK_PDF, SCARICA_PDF`

    2. Dopo la gestione di FESR_OUTPUT (riga 86), appendi il seguente blocco di parsing — PRIMA della riga vuota che chiude la funzione:

       val = os.environ.get("FESR_SCARICA_LINK_PDF")
       if val:
           SCARICA_LINK_PDF = val.strip().lower() in ("true", "1", "yes")
       val = os.environ.get("FESR_SCARICA_PDF")
       if val:
           SCARICA_PDF = val.strip().lower() in ("true", "1", "yes")
       # Se SCARICA_PDF è abilitato, i link sono prerequisito fisico del download
       if SCARICA_PDF:
           SCARICA_LINK_PDF = True

    Bool parsing: `val.strip().lower() in ("true", "1", "yes")` — coerente con le linee guida di CONTEXT.md.
    Non modificare le righe hardcoded `SCARICA_LINK_PDF = False` / `SCARICA_PDF = False` a livello di modulo (righe 57-58): restano come default.
  </action>

  <verify>
    <automated>python -c "
import os, importlib, sys
sys.path.insert(0, '.')

# Test 1 — FESR_SCARICA_LINK_PDF=true
os.environ.clear()
os.environ['FESR_SCARICA_LINK_PDF'] = 'true'
import scraper; scraper.SCARICA_LINK_PDF = False; scraper.SCARICA_PDF = False
scraper.applysecrets()
assert scraper.SCARICA_LINK_PDF is True, 'Test 1 FAILED'

# Test 2 — FESR_SCARICA_PDF=1
importlib.reload(scraper)
os.environ.clear()
os.environ['FESR_SCARICA_PDF'] = '1'
scraper.applysecrets()
assert scraper.SCARICA_PDF is True, 'Test 2 FAILED'
assert scraper.SCARICA_LINK_PDF is True, 'Test 2 implication FAILED'

# Test 3 — nessuna env var
importlib.reload(scraper)
os.environ.clear()
scraper.applysecrets()
assert scraper.SCARICA_LINK_PDF is False, 'Test 3 FAILED (link)'
assert scraper.SCARICA_PDF is False, 'Test 3 FAILED (pdf)'

print('ALL TESTS PASSED')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - applysecrets() dichiara SCARICA_LINK_PDF e SCARICA_PDF nel global statement
    - FESR_SCARICA_LINK_PDF e FESR_SCARICA_PDF vengono letti con os.environ.get()
    - Il parsing usa `.strip().lower() in ("true", "1", "yes")`
    - Se SCARICA_PDF diventa True, SCARICA_LINK_PDF viene forzato a True (implicazione prerequisito)
    - Env var assente o vuota lascia le variabili invariate (False di default)
    - Tutti e sei i test behavior passano
    - `python scraper.py` termina senza errori sintattici
  </acceptance_criteria>

  <done>scraper.py ha applysecrets() che legge FESR_SCARICA_LINK_PDF e FESR_SCARICA_PDF con bool parsing corretto e implicazione prerequisito. Test inline passano.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → scraper globals | Variabili di ambiente GitHub Actions controllate dal repository owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | FESR_SCARICA_PDF env var | accept | Env var settata solo dal repository owner in GitHub Secrets / Actions config; nessun input esterno non fidato |
| T-04-02 | Information Disclosure | Download PDF da URL del portale | accept | URL già pubblici nel portale istituzionale; nessun dato sensibile esposto |
</threat_model>

<verification>
python scraper.py (con env var non impostate) deve terminare con exit 0 e comportamento identico a prima della modifica.
</verification>

<success_criteria>
- `grep -n "FESR_SCARICA_LINK_PDF\|FESR_SCARICA_PDF" scraper.py` ritorna almeno 2 match nella funzione applysecrets()
- `grep -n "global.*SCARICA_LINK_PDF" scraper.py` ritorna 1 match
- I test inline del task passano
</success_criteria>

<output>
Create `.planning/phases/04-pdf-download/04-01-SUMMARY.md` when done
</output>
