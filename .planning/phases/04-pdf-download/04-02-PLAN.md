---
phase: "04-pdf-download"
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .github/workflows/fesr_scraper.yml
autonomous: true
requirements:
  - PDF-01

must_haves:
  truths:
    - "Ogni run cron del workflow scarica i PDF senza modifiche manuali"
    - "Il passo 'Esegui scraper' passa FESR_SCARICA_LINK_PDF=true e FESR_SCARICA_PDF=true allo script"
    - "Se non ci sono delibere con link PDF, il run termina senza errori"
  artifacts:
    - path: ".github/workflows/fesr_scraper.yml"
      provides: "Env var FESR_SCARICA_LINK_PDF e FESR_SCARICA_PDF nel passo Esegui scraper"
      contains: "FESR_SCARICA_PDF"
  key_links:
    - from: ".github/workflows/fesr_scraper.yml step 'Esegui scraper'"
      to: "scraper.py:applysecrets()"
      via: "env block FESR_SCARICA_PDF: \"true\""
      pattern: "FESR_SCARICA_PDF"
---

<objective>
Aggiunge `FESR_SCARICA_LINK_PDF: "true"` e `FESR_SCARICA_PDF: "true"` al blocco `env:` del passo `Esegui scraper` in `fesr_scraper.yml`, abilitando il download PDF in ogni run automatico del workflow.

Purpose: Attiva il download PDF nell'automazione settimanale senza ulteriori interventi manuali.
Output: `.github/workflows/fesr_scraper.yml` con le due env var nel passo `Esegui scraper`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pdf-download/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere FESR_SCARICA_LINK_PDF e FESR_SCARICA_PDF al passo Esegui scraper</name>
  <files>.github/workflows/fesr_scraper.yml</files>

  <read_first>
    - .github/workflows/fesr_scraper.yml (righe 63-68): passo "Esegui scraper" con il suo blocco env esistente
  </read_first>

  <action>
    Nel file `.github/workflows/fesr_scraper.yml`, individua il passo `Esegui scraper` (riga 63).
    Il suo blocco `env:` attuale contiene:

        env:
          FESR_KEYWORD: ${{ github.event.inputs.keyword || 'FESR' }}
          # FESR_DATA_DA e FESR_DATA_A iniettate dal passo precedente via GITHUB_ENV

    Aggiungi le due righe dopo `FESR_KEYWORD`, preservando indentazione YAML a 10 spazi (allineata a FESR_KEYWORD):

        env:
          FESR_KEYWORD: ${{ github.event.inputs.keyword || 'FESR' }}
          FESR_SCARICA_LINK_PDF: "true"
          FESR_SCARICA_PDF: "true"
          # FESR_DATA_DA e FESR_DATA_A iniettate dal passo precedente via GITHUB_ENV

    Valori stringa "true" (con virgolette) — coerente con il parsing bool in applysecrets().
    Non modificare altri passi, secrets, o variabili del workflow.
  </action>

  <verify>
    <automated>python -c "
import yaml, sys
with open('.github/workflows/fesr_scraper.yml', 'r', encoding='utf-8') as f:
    wf = yaml.safe_load(f)
jobs = wf.get('jobs', {})
steps = []
for job in jobs.values():
    steps.extend(job.get('steps', []))
esegui = next((s for s in steps if s.get('name') == 'Esegui scraper'), None)
assert esegui is not None, 'Passo Esegui scraper non trovato'
env = esegui.get('env', {})
assert 'FESR_SCARICA_LINK_PDF' in env, 'FESR_SCARICA_LINK_PDF mancante'
assert 'FESR_SCARICA_PDF' in env, 'FESR_SCARICA_PDF mancante'
assert str(env['FESR_SCARICA_LINK_PDF']).lower() == 'true', f'FESR_SCARICA_LINK_PDF non e true: {env[\"FESR_SCARICA_LINK_PDF\"]}'
assert str(env['FESR_SCARICA_PDF']).lower() == 'true', f'FESR_SCARICA_PDF non e true: {env[\"FESR_SCARICA_PDF\"]}'
print('YAML VALID — entrambe le env var presenti e valorizzate true')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - Il passo "Esegui scraper" contiene `FESR_SCARICA_LINK_PDF: "true"` nel blocco env
    - Il passo "Esegui scraper" contiene `FESR_SCARICA_PDF: "true"` nel blocco env
    - Il file YAML è sintatticamente valido (python yaml.safe_load non lancia eccezioni)
    - Gli altri passi del workflow e le env var esistenti (FESR_KEYWORD, secrets) sono intatti
    - Il test automated passa senza errori
  </acceptance_criteria>

  <done>fesr_scraper.yml ha FESR_SCARICA_LINK_PDF="true" e FESR_SCARICA_PDF="true" nel passo Esegui scraper. YAML valido. Test pytest-free inline passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow env → processo scraper | Variabili hardcoded nel file workflow; controllate dal repository owner via PR/commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | fesr_scraper.yml env block | accept | File YAML in repository protetto; modifiche richiedono push/PR da owner |
| T-04-04 | Denial of Service | Download PDF da portale istituzionale | accept | PAUSA_SECONDI=0.4 già presente in scraper.py; portale pubblico, nessun rate limit dichiarato |
</threat_model>

<verification>
- `grep -c "FESR_SCARICA_PDF" .github/workflows/fesr_scraper.yml` restituisce 2 (una per variabile)
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/fesr_scraper.yml'))"` termina senza errori
</verification>

<success_criteria>
- `grep "FESR_SCARICA_LINK_PDF\|FESR_SCARICA_PDF" .github/workflows/fesr_scraper.yml` mostra entrambe le righe sotto il passo Esegui scraper
- Il workflow YAML è valido
- Nessun'altra env var o step risulta modificato
</success_criteria>

<output>
Create `.planning/phases/04-pdf-download/04-02-SUMMARY.md` when done
</output>
